# Source-video decode for v2v / timeline loading uses PyAV, which ships with the
# ComfyUI portable build. OpenCV is intentionally NOT a dependency: the portable
# launcher runs `python -s`, which hides the user-level site-packages where cv2
# is installed, so `import cv2` fails inside ComfyUI.
av>=13.0

# Source-audio extract + incremental segment mp4 encode (「分段导出」边生成边保存)
imageio-ffmpeg>=0.4

# Smart shot split (智能分割)
scenedetect>=0.6.4,<0.8

# Optional: Refine upscale_method=nvidia_rtx_vsr (NVIDIA RTX GPU only).
# Do not add as a hard dep — AMD / cloud / no-VSR installs would break.
# pip install nvidia-vfx --extra-index-url https://pypi.nvidia.com
